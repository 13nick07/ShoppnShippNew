info:
  name: GET Warehouse
  type: http
  seq: 7

http:
  method: get
  url: http://localhost:3000/api/warehouses
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
