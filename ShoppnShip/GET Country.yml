info:
  name: GET Country
  type: http
  seq: 2

http:
  method: get
  url: http://localhost:3000/api/countries
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
